# Dependencies
node_modules/

# Compact compiler output (generated by `npm run compile`).
# The deployed contract bundle below is intentionally TRACKED: it is a hard
# build/runtime dependency of the frontend (types, contract JS, zkir, keys) and
# the `compact` compiler is not installable in CI, so it cannot be rebuilt there.
contracts/managed/*
!contracts/managed/confidential_salary_benchmarking/

# Local state — NEVER commit these (they contain wallet keys / seeds)
.midnight-state.json
.midnight-wallet-state/
midnight-level-db/
*.env.local

# Frontend build output
frontend/dist/
frontend/.vite/

# Logs & misc
*.log
.DS_Store
coverage/